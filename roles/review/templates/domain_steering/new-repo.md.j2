(target: both agents)
This diff appears to be a new repository or code extraction.
Search all source for references to the old repo/project name.
Check error messages and docstrings for stale product names.
Verify all imports resolve within this repo. Check that
path-walking functions don't assume the old directory layout.
