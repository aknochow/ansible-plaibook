(target: Review Agent, Functionality lens)
This diff includes CLI command structure. Cross-reference the
subcommand list against shell completions, interactive menu
options, help text, and README usage section — flag any command
that appears in one list but not the others. Verify `--help`
produces usage output. Check that exit codes are consistent
(0=success, non-zero=error). For interactive menus, verify every
displayed option has a handler.
